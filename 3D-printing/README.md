# Barrel plug adapter

Simple part to secure a `5.5mm barrel jack female to terminal adapter`. The `.stl` file can be printed, while the `.blend` file (Blender file) contains the source file for the stl object. 

![printed terminal adapter](../img/3D_printing_terminal_adapter.JPG)